using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		// プレハブを取得
		// GameObject prefab;
		// プレハブからインスタンスを生成
		//GameObject coin = Instantiate (prefab, transform.position, Quaternion.identity) as GameObject;
		//CoinScript c = coin.GetComponent<CoinPrefab>;
		//c.Create ();
	}
}
